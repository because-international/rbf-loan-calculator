import ReactDOM from 'react-dom/client';
import './index.css';
import RBFCalculator from './App.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <RBFCalculator />
);
